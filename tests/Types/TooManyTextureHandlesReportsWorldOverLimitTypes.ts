export interface TextureHandleCase {
    readonly name: string;
    readonly handleCount: number;
    readonly nested: boolean;
    readonly worldCount: number;
    readonly expectFinding: boolean;
}
